import React from 'react'

function Buttons(props) {
  return (
  //  props.showButton ? <button>{props.buttonName}</button> : <div></div>
   <button>{props.buttonName}</button>
  )
}

export default Buttons